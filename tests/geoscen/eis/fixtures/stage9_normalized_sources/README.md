Stage 9 tests build normalized source artifacts into temporary directories from the accepted Stage 8 offline fixtures.
This directory is reserved for static normalized-source fixtures if a future approval requires them.
