name: daily_fx_cross_update_t2

on:
  workflow_run:
    workflows: ["fx_spot_update_t2"]
    types: [completed]

concurrency:
  group: fx-cross-t2
  cancel-in-progress: false

jobs:
  update_cross:
    runs-on: ubuntu-latest
    environment: production
    if: |
      github.event.workflow_run.conclusion == 'success' &&
      github.event.workflow_run.head_branch == 'main'

    env:
      PYTHONPATH: ${{ github.workspace }}/src
      R2_ENDPOINT: ${{ secrets.R2_ENDPOINT }}
      R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
      R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
      R2_BUCKET: ${{ secrets.R2_BUCKET }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install deps (CI)
        run: |
          python -m pip install --upgrade pip
          python -m pip install --no-cache-dir -r requirements.ci.txt

      - name: Update NZDAUD cross
        run: python -m spine.jobs.fx.build_fx_cross_update_t2

      - name: Validate NZDAUD cross
        run: python -m spine.jobs.fx.validate_fx_cross_t2